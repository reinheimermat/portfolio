---
import { cn } from "../../utils/cn";

interface Props {
    class?: string;
}

const props = Astro.props;
---

<div class={cn("container mx-auto max-w-[64rem] px-4", props.class)}>
    <slot />
</div>
